$[onebit]
#include <Pixels_$[iface].h>
$[antialiasing]
#include <Pixels_$[controller].h>

#define null NULL

Pixels pxs($[width], $[height]);

$[declarations]

$[functions]

	void setup() {
		$[hint]
		pxs.init();
$[sketchcode_s]
	}

	void loop() {
$[sketchcode_l]
	}

$[resources]
